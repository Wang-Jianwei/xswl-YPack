# YAML -> Mermaid 可视化生成器 🔧

本工具生成一个 Mermaid `flowchart`，用于可视化 ypack 的 YAML 配置（应用信息、安装设置、变量、文件、包/组件及其层次结构等）。

## 使用方法 ✅

生成 Mermaid 源码文件：

```bash
python -m tools.yaml_to_mermaid examples/full_example.yaml -o tmp/full_example.mmd
```

然后可以在 [mermaid.live](https://mermaid.live/) 或 VS Code 的 Mermaid 预览扩展中打开生成的 `.mmd` 文件查看图表。

## 设计说明 💡

- 默认生成 `flowchart LR`（从左到右）
- 会把 `app` 节点作为中心，连接 `install`、`variables`、`files`、`packages` 等
- `packages` 会以每个组件为节点并用 `--&gt;` 表示父子关系

## 扩展建议 ✨

- 可添加 `--format png` 选项，使用 `mmdc`（mermaid-cli）直接输出图片
- 增强节点样式（不同颜色显示可选项、默认项）
- 支持将 `packages` 作为 `subgraph` 分组显示

